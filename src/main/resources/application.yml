spring:
  application:
    name: fantasy-f1-api

  datasource:
    url: jdbc:postgresql://localhost:5432/REDACTED
    username: REDACTED
    password: REDACTED
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081